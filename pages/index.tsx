import Header from "@/components/Header";
import Layout from "@/components/Layout";
import React from "react";

const Home = () => {
  return (
    <Layout>
      <Header />
    </Layout>
  );
};

export default Home;
